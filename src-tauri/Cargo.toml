[package]
name = "time-tracker"
version = "0.1.0"
description = "Time Tracker — почасовой трекер времени"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "time-tracker"
path = "src/main.rs"
